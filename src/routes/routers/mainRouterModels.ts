export type authDataModel = {
    email: string,
    password: string,
    name: string,
    imageURL?: string,
}

export type loginUserModel = {
    email: string,
    password: string,
}

export type userWithToken = {

}